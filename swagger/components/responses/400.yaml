type: object
required:
  - status
  - message
  - data
properties:
  status:
    type: integer
    example: 400
  message:
    type: string
    example: BadRequest
  data:
    type: object
    required:
      - message
    properties:
      message:
        type: string
        example: "Invalid period format. Expected YYYY-MM."
